package simulador_notas.simuladordenotas.crontolador;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.DeleteMapping;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import simulador_notas.simuladordenotas.modelos.usuariomodels;

import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

@RestController
@RequestMapping(path = "/usuario")

public class usuareiocontrolador {
    
    @Autowired
    usuareiocontrolador usuariorepositorio;
    
    @GetMapping(path="/all")
    public Iterable<usuariomodels> getAllusuarios(){
       return usuariorepositorio.findAll();
    }

    private Iterable<usuariomodels> findAll() {
        return null;
    }

    @PostMapping(path = "/save")
    public usuariomodels savePaciente(@RequestBody usuariomodels usuario) {
        return usuariorepositorio.save(usuario); 
    }

    private usuariomodels save(usuariomodels usuario) {
        return null;
    }

    @DeleteMapping(path = "/delete/{id}")
    public void deleteusuarioById(@PathVariable("id") int id){
        usuariorepositorio.deleteusuarioById(id);
    }

}
